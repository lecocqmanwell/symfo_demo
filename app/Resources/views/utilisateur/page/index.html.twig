{% extends 'utilisateur/layout.html.twig' %}


{% block main %}
    <h1>{{ user.username }}</h1>

    <p>{{user.email}}</p>

    <p>{{ user.adress.town }}</p>

    <a href="{{ path('utilisateur_edit', { id: user.id }) }}" class="btn btn-sm btn-primary">
        <i class="fa fa-edit" aria-hidden="true"></i> {{ 'action.edit'|trans }}
    </a>

{% endblock %}
